const express = require('express');
const app = express();






const PORT = 3000;
app.listen(PORT, (err) => {
    if (error) {
        throw error
    }
    console.log(`Pokemon inventory server started - listening on port ${3000}`);
})